<h1> Room : <%= @room.name %> </h1>

<div class="messages"> 
    <%= render @messages %>
</div>

<%= form_for [@room, @message], :remote => true do |f| %>
    <%= f.label :content %>
    <%= f.text_field :content %>
    <%= f.submit %>
<% end %>

<script type="text/javascript" charset="utf-8">
//  setInterval(updateMessagesViaJSON, 3000)
 setInterval(updateMessagesViaJS, 3000)
</script>